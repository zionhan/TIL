const add = ( a, b ) => { 
    return a + b
}

const add2 = ( a, b ) => a + b;


const hello = ( name ) => {
    console.log( `Hello, ${name}!` );
}

const sum = add( 1, 2 );
const sum2 = add2( 3, 2 );
console.log( sum )
console.log( sum2 )

hello( "hando" )



baby = '한시온';
console.log( `한도경의 아들은 ${baby}이다.` );