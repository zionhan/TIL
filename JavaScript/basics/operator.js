let value = 1;
let a = 1 + 2 - (3 *4) / 4
console.log( a );
console.log( a++ );
console.log( a );
console.log( ++a );


// NOT !
// AND &&
// OR ||

const b = true;
console.log( !b || b );

const c = 2;
const d = 2;
const equals = c==d;    // '==' 타입을 검사하지 않는다
const equals2 = c===d;  // '===' 3번 입력하는 것을 선호!!!
// '!==', '===', '>', '<', '>=', '<='
console.log( equals )

